# 99. 해야할일

기준 경로: `focusai-webapp`  
갱신일: 2026-05-19

이 문서는 현재 코드와 문서 점검 결과를 기준으로 남은 작업을 우선순위별로 정리한다.

## 2026-05-18 처리 결과

- `focusai-webapp/deploy/prod.env.example`에서 실제로 보이는 MariaDB, Anthropic, SMTP 값을 제거하고 placeholder로 교체했다.
- `npm test`를 서버 API 테스트, Discord 봇 회귀 테스트, 문서성 테스트를 모두 실행하는 대표 명령으로 추가했다.
- GitLab CI `verify` 단계가 `npm ci`, `npm run lint`, `npm test`, `npm run build`를 모두 실행하도록 바뀌었다.
- `docs/risk-hardening.test.ts`의 운영 env placeholder 검증이 `npm test`에 포함되어 같은 유형의 노출을 로컬 검증에서 잡는다.
- DB와 uploads 볼륨을 하나로 묶는 백업/복구 절차를 `08_운영_배포.md`에 추가했다.
- `AUTO_MIGRATE_SCHEMA` 운영 사용 기준과 DDL 권한 최소화 전환 절차를 문서화했다.
- `MARIADB_PASSWORD`, `ANTHROPIC_API_KEY`, `SMTP_PASSWORD`, `DISCORD_BOT_TOKEN`, `DISCORD_BOT_SETTINGS_SECRET` 회전 절차를 문서화했다.
- 세션 데이터, AI 분석, 문의, 감사 로그, 공지 첨부 파일의 기본 보존 정책을 `03_데이터베이스_설계.md`에 추가했다.
- 아래 항목 중 운영 Discord 서버, 실제 배포 서버 `.env`, 실제 시크릿 회전처럼 외부 권한이 필요한 항목은 로컬에서 완료 처리하지 않았다.

## 현재 진행 상태 요약

- 로컬 검증 체계: 완료
- CI verify: 현재 lint 중심, test/build 연결 여부 재결정 필요
- 운영 env 예시 파일 정리: 완료
- 운영 서버 실제 secret 점검과 회전: 대기
- 운영 Discord 봇 권한과 명령 14개 실서버 검증: 대기
- 백업/복구 절차와 데이터 보존 정책 문서화: 완료
- 실제 백업 복구 리허설: 대기
- 통합 테스트 보강: 대기

다음 우선순위는 운영 접근 권한이 있으면 secret 회전과 Discord 봇 실서버 검증이고, 로컬 개발만 가능하면 관리자/공지/문의 API 통합 테스트 보강이다.

## 바로 할 일

- [ ] 운영 환경의 실제 시크릿을 점검하고, 노출 가능성이 있는 값은 회전한다.
- [x] `deploy/prod.env.example`에는 더미 값만 남아 있는지 확인한다.
- [ ] 배포 서버의 `DISCORD_BOT_SETTINGS_SECRET`이 고정값으로 설정되어 있는지 확인한다.
- [ ] FocusAI Discord 봇의 관리자 사용자 ID와 역할 ID가 운영 서버에 정확히 들어갔는지 확인한다.
- [x] Discord 알림 채널 설정 파일 경로가 영구 볼륨에 있는지 확인한다.
- [x] `npm test`, `npm run build`, `npm run lint`를 배포 전 로컬 필수 검증으로 묶는다.
- [ ] GitLab CI verify에 `npm test`, `npm run build`를 다시 포함할지 결정한다.
- [ ] 문서 변경분을 별도 커밋으로 정리할지 결정한다.

## 기능 검증

- [ ] FocusAI Discord 봇 명령 14개를 운영 Discord 서버에서 직접 실행해 본다.
- [ ] `focus상태`가 서버 상태와 버전 정보를 정상 응답하는지 확인한다.
- [ ] `focus요약`이 오늘, 최근 7일, 최근 30일 지표를 정상 출력하는지 확인한다.
- [ ] `문의목록`, `문의상세`, `문의처리`가 실제 지원 문의 상태를 정확히 반영하는지 확인한다.
- [ ] `사용자조회`, `최근세션`, `감사로그`가 권한 있는 관리자에게만 응답하는지 확인한다.
- [ ] `공지등록`, `공지목록`, `공지수정`, `공지종료`가 웹 관리자 화면과 같은 데이터를 변경하는지 확인한다.
- [ ] `알림채널설정`, `알림채널목록`이 재시작 후에도 설정을 유지하는지 확인한다.
- [ ] Lost Ark Discord 봇이 FocusAI 관리 봇 설정과 충돌하지 않는지 확인한다.

## 테스트와 CI

- [ ] GitLab CI 검증 단계에 `npm test`를 추가할지 결정한다.
- [ ] GitLab CI 검증 단계에 `npm run build`를 추가할지 결정한다.
- [ ] 서버 API 테스트가 CI에서 항상 실행되도록 할지 결정한다.
- [ ] Discord 봇 회귀 테스트가 CI에서 항상 실행되도록 할지 결정한다.
- [ ] 관리자 권한 변경, 마지막 관리자 보호, 공지 변경, 문의 처리 API에 대한 통합 테스트를 보강한다.
- [ ] 카메라 권한 거부, MediaPipe 로딩 실패, AI API 실패 시나리오를 프런트엔드 테스트 또는 수동 검증 항목으로 추가한다.

## 운영

- [x] DB 백업과 업로드 볼륨 백업을 하나의 절차로 문서화한다.
- [ ] 백업 복구 테스트에서 이미지, 첨부 파일, Discord 설정 파일까지 확인한다.
- [x] `AUTO_MIGRATE_SCHEMA` 운영 사용 여부를 결정한다.
- [x] 운영 DB 계정의 DDL 권한을 최소화할 수 있는 마이그레이션 절차를 만든다.
- [x] 헬스체크가 DB 연결, 메일 설정, AI 설정, Discord 봇 상태를 어느 수준까지 확인할지 정한다.
- [ ] Discord API 장애 시 웹 서버와 핵심 API가 계속 동작하는지 운영 로그로 확인한다.
- [ ] AI 사용량과 비용 추정치를 월별로 검토할 수 있는 운영 리포트를 만든다.

## 보안

- [x] `JWT_SECRET`, `DISCORD_TOKEN`, `OPENAI_API_KEY`, `SMTP_PASS`, `MARIADB_PASSWORD` 회전 절차를 문서화한다.
- [ ] 관리자 권한 변경 내역을 주기적으로 감사하는 운영 절차를 만든다.
- [ ] 관리자 Discord 명령 실행 로그를 별도로 추적할지 결정한다.
- [ ] 다중 인스턴스 운영 전 공유 저장소 기반 rate limit을 검토한다.
- [x] CORS 허용 origin을 운영 도메인으로 제한한다.
- [x] 쿠키 기반 인증 운영 시 `SESSION_COOKIE_SECURE=true`, `TRUST_PROXY=true`를 확인한다.

## 코드 구조

- [ ] `src/App.tsx`를 인증, 세션, 관리자, 게시판, 공지 화면 단위로 점진 분리한다.
- [ ] `server/src/store.ts`를 사용자, 세션, 문의, 공지, 감사 로그, AI 사용량 저장소로 분리한다.
- [ ] 프런트엔드와 백엔드의 계약 타입을 공용 모듈 또는 코드 생성 방식으로 통합한다.
- [ ] `db/schema.sql`과 런타임 스키마 보정 로직의 차이를 줄인다.
- [ ] FocusAI Discord 봇 명령 핸들러를 명령별 모듈로 분리할지 검토한다.
- [ ] 운영 규모가 커질 경우 Discord 봇을 별도 워커 프로세스로 분리한다.

## 프런트엔드

- [ ] 카메라 권한 실패 안내를 더 명확히 만든다.
- [ ] MediaPipe 모델 로딩 실패 시 재시도와 대체 안내를 보강한다.
- [ ] 모바일 브라우저에서 세션 화면, 관리자 화면, 게시판 화면을 수동 점검한다.
- [ ] 긴 세션 리포트 PDF 생성 성능을 측정한다.
- [ ] 관리자 화면에서 Discord 봇 상태와 알림 채널 설정 상태를 보여줄지 검토한다.

## 데이터 정책

- [x] 계정 삭제 시 감사 로그와 문의 이력을 삭제, 익명화, 보존 중 어떤 방식으로 처리할지 결정한다.
- [x] 세션 데이터 보존 기간과 사용자 삭제 정책을 명확히 문서화한다.
- [x] AI 분석 원문과 결과 저장 범위를 개인정보 정책과 맞춘다.
- [x] 공지와 지원 문의 첨부 파일의 보존 기간을 정한다.

## 배포 전 점검 명령

```bash
npm ci
npm run lint
npm test
npm run build
```

운영 서버에서는 배포 후 다음 항목을 확인한다.

- [ ] `/api/health` 응답이 정상이다.
- [ ] 로그인과 세션 쿠키가 정상 동작한다.
- [ ] 카메라 권한 허용 후 집중 세션이 시작된다.
- [ ] AI 분석 요청이 성공하거나 설정 누락 오류를 명확히 반환한다.
- [ ] 관리자 화면에서 사용자, 세션, 문의, 공지, 감사 로그가 조회된다.
- [ ] FocusAI Discord 봇이 명령 등록과 관리자 권한 검사를 정상 수행한다.


포커스 AI 수정

- 로그인한후 사이트 껐다 켜도 로그인 되있는 상황
- 로그인 페이지 디자인
- 집중도 주간 보여주는거 달 또는 전주 보일수있게 수정
- 사이트 도메인 명 수정, 사이트 로고
- 학습 세션에서 관찰화면 부분 고정해두고 오른쪽 카드 목록만 스크롤해서 내릴수있도록 설정

아직 안 끝난 것:

- `focusai-webapp/apk-packages/`의 local/server debug APK를 실제 사용자 전달 경로에 올릴지 결정
- 실제 안드로이드폰을 USB 디버깅/ADB로 연결해서 APK 설치 후 실행 확인
- 실제 폰에서 카메라 권한 허용, 집중도 트래킹 세션 확인
